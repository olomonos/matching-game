import * as React from 'react';

export const Rules: React.StatelessComponent = () => {

    return (
        <div className='rules-box'>
            <span>You should find the odd circle in the right part of the field.</span>
        </div>
    )
};